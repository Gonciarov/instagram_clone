<div class="container">
<div class="row">
<div class="col-8">
<%= "#{@posts.size.to_s} posts found" %>
<% @posts.each do |post| %>
    <%= post.account.username %>
<%= image_tag post.image.url, class: "img img-fluid" %>
<% end %>
</div>
</div>
</div>